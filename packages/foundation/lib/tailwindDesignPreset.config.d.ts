export const prefix: string;
export namespace theme {
    const colors: {};
    const borderRadius: {};
    const spacing: {};
    const animation: {};
    const boxShadow: {};
    namespace fontFamily {
        const sans: string[];
    }
}
export const plugins: never[];
